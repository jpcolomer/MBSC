<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @desarrollo_organizacional.cuadros_reemplazo %></h1>
    <a href="<%= url_for :action => :show, :id => @desarrollo_organizacional.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @desarrollo_organizacional.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @desarrollo_organizacional.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="desarrollo_organizacional[cuadros_reemplazo]" class="fieldLabel">Cuadros reemplazo</label>
            <input type="text" id="desarrollo_organizacional[cuadros_reemplazo]" name="desarrollo_organizacional[cuadros_reemplazo]" value="<%= @desarrollo_organizacional.cuadros_reemplazo %>" <%= placeholder( "Cuadros reemplazo" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
